# Project Tasks

## Frontend Tasks

### Create the starter UI frontent
- Connect the frontend with the websocket for realtime straming of the response
- Design the `Auth page` for the application 
- Add the options for the social auth
- Users Profile Page 



## Backend Tasks

### User Endpoints 
- Create the Users `list` ALL USERS endpoint
- Create the Users `GET` endpoint
- Create the Users `POST` endpoint
- Create the Users `UPDATE` endpoint
- Create the Users `DELETE` endpoint


### Sessions Endpoints
- Create an endpoint that accept the `users speech and do the analysis`
- Create the Sessions list endpoint that will get all the session for the perticular user
- 


### Authentication Endpoints 
- Create the authentication endpoints which is going to login the user
- Add the social Auth feature `Optional for now`



## Database Schema
- Design the `Users`(modal) to store the data in database `Postgresql` as Database.
- Design the `Session` (modal) Schema that will store the user speech data like speech text,audio files etc. which is helpful for the analysis in the profile section and Details analysis like monthly improvement or improvements highlighs.

- Design the `Speech Result Model` to store the analysis results 

- Design the `Questions Model` that will store the questions (can be ignore for now as not that necessary for now )


# Completed Taks so far
## Frontend
- Design the Home Page UI.
- Design the Practice Page for the speech audio delivery
- Design the report Page partiall for just one tab. `not done yet` on hold due to backend api is not ready for this section

## Backend 
- Setup the basic fastapi like postgresql and project structure 
### User Specific 
- Add the Auth logic for the user with `JWT`
- Add the user creation and list of user

### Sessions 
- Setup the RabbitMQ,Redis and Celery to shift the heavy task like report generation and give an live updates by redis database.
- Created the api for the audio resport 
- Websocket Api for realtime stramming of response which is generated by the background task in Celery


## Database 

- Design the ER diagram for the entire application.
- Add the Users Table and Session Table in the database  