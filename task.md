# Project Tasks

## Frontend Tasks

### Create the starter UI frontent
- Design the `Practice page`
- Design the `Home page`
- Design the `Auth page` for the application 
- Add the options for the social auth
- Users Profile Page 



## Backend Tasks
- [X] Setup the basic FastApi project structure.
- [X] Create an Basic Endpoint 

### User Endpoints 
- [x] Create the Users `list` ALL USERS endpoint
- [x] Create the Users `GET` endpoint
- [x] Create the Users `POST` endpoint
- Create the Users `UPDATE` endpoint
- Create the Users `DELETE` endpoint


### Sessions Endpoints
- Create an endpoint that accept the `users speech and do the analysis`
- Create the Sessions list endpoint that will get all the session for the perticular user
- 


### Authentication Endpoints 
- Create the authentication endpoints which is going to login the user
- Add the social Auth feature `Optional for now`



## Database Schema
- [X] Design the `Users`(modal) to store the data in database `Postgresql` as Database.
- [X] Design the `Session` (modal) Schema that will store the user speech data like speech text,audio files etc. which is helpful for the analysis in the profile section and Details analysis like monthly improvement or improvements highlighs.

- Design the `Speech Result Model` to store the analysis results 

- Design the `Questions Model` that will store the questions (can be ignore for now as not that necessary for now )
